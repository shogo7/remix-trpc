// client/tsconfig.json
{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "types": ["@remix-run/node", "vite/client"],
    "moduleResolution": "Bundler", // Vite に最適な形式,
    "noEmit": true
  },
  "include": [
    "./**/*",
    "../server/types" // ← 明示的に含める (ちなみにserverだけにしてもできるか試す。)
  ],

}
